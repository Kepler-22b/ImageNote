{% extends 'AppBundle::index.html.twig' %}

{% block title %}
    Posts
{% endblock %}

{% block content %}
    <ul>
    {% for post in posts %}
        <li><a href="{{ path('post', {id: post.id}) }}">{{ post.title }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}